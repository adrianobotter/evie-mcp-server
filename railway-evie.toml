[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile.evie"

[deploy]
startCommand = "python -m src.evie.server"
healthcheckPath = "/health"
healthcheckTimeout = 60
restartPolicyType = "on_failure"

[settings]
rootDirectory = "/"
